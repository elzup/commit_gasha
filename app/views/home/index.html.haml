%h1 コミットガシャ

%p カード一覧

// NOTE: ログイン状態の記述順序は正しいか？
- if @user
  %p= link_to 'ログアウト', '/logout'
  %p= link_to_if @user.can_turn_card?, 'ガシャを引く', '/gasha'
  %p= link_to 'コミットインポート', '/import'
  = render 'shared/user_info'
  = render @user.cards
- else
  %p= link_to 'ログイン', '/login'
