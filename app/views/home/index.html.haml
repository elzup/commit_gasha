= render 'shared/top', user: @user, title: 'Home'

- if @user
  %div.card-list
    %h2 ランダム PickUp
    = render @random_cards
  %div.card-list
    %h2 取得カード
    = render @user.cards
- else
  %p= link_to 'ログイン', '/login'
